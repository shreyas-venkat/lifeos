# Credentials — never commit real secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbbs.db

# Editors
.vscode/settings.json
.idea/
*.swp
*.swo

# Node
node_modules/
npm-debug.log*

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Claude Code — local overrides (personal, never shared)
.claude/settings.local.json

# Claude Code — project hook config (machine-specific, never shared)
# Commit project.env.example instead
.claude/project.env

# Hook runtime logs (generated, not tracked)
.claude/logs/
